/**
 * @format
 */

import 'react-native-gesture-handler';
import {AppRegistry} from 'react-native';
import {name} from './package.json';
import Example from './Example/Example';

AppRegistry.registerComponent(name, () => Example);
